version: "3"
services:
  squid:
    container_name: io.squid
    build:
      context: ./etc/squid
    network_mode: host
    ports:
      - '3128:3128'
    volumes:
      - ./etc/squid/config:/etc/squid:rw
      - ./var/cache:/var/cache/squid